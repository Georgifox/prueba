<?php
//crear funcion que genere num aleatorios parametros
    //array que se llenara con los nums
    //num min y num max 
    //num de numeros a generar
    //true para que se puedan repetir y false para que no lo hagan

    function numAleatorios($array,$min,$max,$cantidad,$repetidos){
        for ($i=0; $i <=$cantidad ; $i++) { 
            $array = range($min, $max)
                if($repetido==false){
                    foreach ($array as $i=> $num) {
                        # code...
                    }
                }
        }
    }

?>